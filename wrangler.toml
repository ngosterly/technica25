# Cloudflare Pages configuration for Node.js compatibility
name = "decisionera"
compatibility_flags = ["nodejs_compat"]
compatibility_date = "2024-11-16"

# This enables Node.js built-in modules required by Firebase Admin SDK
# Including: crypto, fs, path, util, net, tls, buffer, stream, etc.
